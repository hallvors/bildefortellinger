
<div class="siema">
{{#each images}}
	<div class="slide">
		<img src="{{this}}" />
	</div>
{{else}}
	<p class="error">Hei{{#if name}} {{name}}{{/if}}! Vi fant ingen bilder å fortelle om. Kan du be om lenke til bildene på nytt?</p>
{{/each}}
</div>
{{#if name}}
<input type="hidden" value="{{name}}" id="name">
<input type="hidden" value="{{project}}" id="project">
{{/if}}
<nav id="toolbar"></nav>
